name: Terraform CI

on:
  pull_request:
    branches:
      - main

env:
  TF_WORKING_DIR: starter/environments/dev

jobs:
  terraform:
    name: Validate & Plan
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      # TODO: Add a step to set up Terraform
      # Use: hashicorp/setup-terraform@v3
      # Pin terraform_version to ">= 1.5"

      # TODO: Add a step to check Terraform formatting
      # Run: terraform fmt -check -recursive
      # This step should fail the pipeline if any .tf files are not formatted.
      # Hint: run from the repo root so all subdirectories are checked.

      # TODO: Add a step to initialise Terraform
      # Run: terraform init -backend=false
      # (Skip backend init in CI — there is no running LocalStack)
      # Set working-directory to ${{ env.TF_WORKING_DIR }}

      # TODO: Add a step to validate the Terraform configuration
      # Run: terraform validate
      # Set working-directory to ${{ env.TF_WORKING_DIR }}

      # TODO: Add a step to run terraform plan
      # Set working-directory to ${{ env.TF_WORKING_DIR }}
      # Use -input=false and -no-color flags
      # Configure the following environment variables for dummy AWS credentials:
      #   AWS_ACCESS_KEY_ID:     test
      #   AWS_SECRET_ACCESS_KEY: test
      #   AWS_DEFAULT_REGION:    ap-southeast-2
